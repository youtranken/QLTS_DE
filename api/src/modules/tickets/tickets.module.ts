import { Module } from '@nestjs/common';

/**
 * Chủ vòng đời ticket/booking (AD-4). Story 3.1a mới đặt nền DB + từ vựng
 * trạng thái (ticket-states.ts); service/controller vào từ 3.1b/3.1c.
 */
@Module({})
export class TicketsModule {}
